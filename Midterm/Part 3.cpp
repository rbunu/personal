#include <iostream>
#include <cmath>
using namespace std;

int main(){
    int x 
    cout<<"enter a two-digit number: ";
    cin >>x;
    int most=x/10
    int least=x%10;

    If(least !=0&& x % least == 0){
        cout <<least<"is a factor of"<<x<<endl;
    }
    int root = sqrt(x);
    if(root*root==x){
        cout<<x<<"is a perfect square"<<endl;
    }
    return 0;
}