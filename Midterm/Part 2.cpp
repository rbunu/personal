Precedence:
Operator precedence defines the rules that determine the order in which operators are evatualed within a complex expression. Operators with higher precedence are executed before operators with lower precedence

Difference between a Char and a String data typede:
A char is a primitive data type that stores exactly one single character and is enclosed in single quotes.
A string is an object that holds a sequence of multiple characters and is enclosed in double quotes

Modulo Operator results:

The modulo operator returns the remainder of a division operation, so the possible resuls range from 0 up to less of than the devisor.

Possibilities for 5 variables:
32 possible combinations